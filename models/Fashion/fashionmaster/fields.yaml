# ===================================
#  Field Definitions
# ===================================

fields:

    title:
        label: Title
        span: auto

    slug:
        label: Slug
        span: auto
        preset:
            field: title
            type: slug
